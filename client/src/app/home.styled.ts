import styled from 'styled-components';

const Body = styled.body`
  background-color: #f4f4f4;
`;

const ScreensLoaderWrapper = styled.div`
  width: 100%;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;

const Styled = { Body, ScreensLoaderWrapper };

export default Styled;
